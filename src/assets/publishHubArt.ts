/** Publish hub - single item entry card artwork. */
export const PUBLISH_SINGLE_ITEM_ART = require('./publish-single-item.png');

/** Publish hub - clearance bundle entry card artwork. */
export const PUBLISH_CLEARANCE_BUNDLE_ART = require('./publish-clearance-bundle.png');

/** Illustration size in publish hub cards (square slot, contain fit). */
export const PUBLISH_HUB_ART_SIZE = 76;
